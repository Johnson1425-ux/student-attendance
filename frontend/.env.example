# Base URL of the backend API.
# Leave unset for local development (Vite proxies /api to localhost:4000).
# On Vercel set this to the Render service URL, e.g.
#   VITE_API_BASE_URL=https://attendance-api.onrender.com
VITE_API_BASE_URL=
